#!/bin/bash
# Script to change localhost and facebook.com resolutions

# Backup the existing /etc/hosts file
sudo cp /etc/hosts /etc/hosts.bak

# Remove existing localhost and facebook.com entries
sudo sed -i '/localhost/d' /etc/hosts
sudo sed -i '/facebook.com/d' /etc/hosts

# Add new localhost and facebook.com entries
echo "127.0.0.2   localhost" | sudo tee -a /etc/hosts > /dev/null
echo "8.8.8.8     facebook.com" | sudo tee -a /etc/hosts > /dev/null
